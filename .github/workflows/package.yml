name: Package

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macOS-latest]
        # os: [macOS-latest, ubuntu-latest]

    steps:
    - uses: actions/checkout@v1
    - name: Install Brew Items
      run: |
        brew install pkg-config
        brew install coreutils
        brew install cpanm
    - name: Install Perl Modules
      run: |
        cpanm -f PAR
        cpanm PAR::Packer
    - name: Package
      shell: bash -v {0}
      run: |
        realpath cpanm
        export PATH="$PATH:`realpath cpanm`"
        echo $PATH
        perl ./test.pl
        which pp
        /usr/local/Cellar/perl/5.30.0/bin/pp -o test2 ./test.pl
        ./test2
        pp -o test ./test.pl
        ./test